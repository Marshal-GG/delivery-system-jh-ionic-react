import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Delivery System JH [Ionic React]',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
